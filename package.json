{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:prod": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/intel-suite run build",
    "start:prod": "NODE_ENV=production DATABASE_POOL_MAX=1 PUBLIC_PORTAL_CONCURRENCY=1 PUBLIC_PORTAL_ROTATION_BATCH_SIZE=3 WEB_INTELLIGENCE_AI_CANDIDATE_LIMIT=20 STRUCTURED_RFP_REVIEW_CANDIDATE_LIMIT=3 MALLOC_ARENA_MAX=2 UV_THREADPOOL_SIZE=2 node --max-old-space-size=224 --max-semi-space-size=8 ./api-server/dist/index.mjs",
    "db:push": "pnpm --filter @workspace/db run push",
    "db:push:intel": "pnpm --filter @workspace/db run push:intel",
    "db:verify-silo": "pnpm --filter @workspace/scripts run verify:database-silo",
    "rfp:sources:validate": "node ./scripts/validate-direct-rfp-portals.mjs",
    "catalogue:completion:audit": "pnpm --filter @workspace/scripts run audit:catalogue-completion",
    "catalogue:live:verify": "pnpm --filter @workspace/scripts run verify:catalogue-live",
    "public-portals:import": "node ./scripts/import-public-portal-sources.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./api-server\" --filter \"./intel-suite\" --filter \"./scripts\" --if-present run typecheck",
    "public-portals:validate": "node ./scripts/validate-public-portal-sources.mjs",
    "statewide:verify-live": "pnpm --filter @workspace/scripts exec tsx ./src/run-statewide-live-verification.ts"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.9.6"
  }
}
